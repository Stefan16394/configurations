spring:
  application:
    name: server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Stefan16394/configurations.git
          search-paths:
            - client
            - client2